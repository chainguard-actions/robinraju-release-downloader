name: 'Build and Test'
on:
  # Trigger manually
  workflow_dispatch:
  pull_request:
  push:
    tags:
      - '*'
    branches:
      - main

permissions:
  contents: read

jobs:
  build:
    strategy:
      matrix:
        os:
          - ubuntu-latest
          - macos-latest
          - windows-latest
    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout Repo
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4

      - name: Setup node and cache dependencies
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Build and test
        shell: bash
        run: |
          npm ci
          npm run all

      - name: Run action
        uses: ./
        id: download-public
        with:
          repository: 'robinraju/probable-potato'
          tag: '1.0.1'
          fileName: 'potato.jpeg'
          tarBall: true
          zipBall: true
          out-file-path: './test-downloads'

      - name: List downloaded files
        shell: bash
        run: ls -lrth test-downloads

      - name: Download tarBall & zipBall only
        uses: ./
        with:
          repository: 'robinraju/probable-potato'
          tag: '1.0.2'
          tarBall: true
          zipBall: true
          out-file-path: './public-download'

      - name: List downloaded files
        shell: bash
        run: ls -lrth public-download

      - name: Test download latest pre-release
        uses: ./
        with:
          repository: 'robinraju/probable-potato'
          latest: true
          preRelease: true
          fileName: 'prerelease.txt'
          tarBall: true
          zipBall: true
          out-file-path: './prerelease-downloads'

      - name: List downloaded files
        shell: bash
        run: ls -lrth prerelease-downloads

      - name: Test download from a private repo
        uses: ./
        id: download-private
        with:
          repository: 'robinraju/release-downloader-test'
          latest: true
          fileName: '*'
          tarBall: true
          zipBall: true
          extract: true
          token: ${{ secrets.RELEASE_DOWNLOADER_TEST_TOKEN }}
          out-file-path: './downloader-test'

      - name: List downloaded files from private repo
        shell: bash
        run: ls -lrth downloader-test

      - name: Read output variable set by previous steps
        shell: bash
        env:
          PUBLIC_TAG_NAME: ${{ steps.download-public.outputs.tag_name }}
          PRIVATE_TAG_NAME: ${{ steps.download-private.outputs.tag_name }}
          DOWNLOADED_FILES: ${{ steps.download-public.outputs.downloaded_files }}
        run: |
          if [ -n "$PUBLIC_TAG_NAME" ]; then
            echo "$PUBLIC_TAG_NAME"
          else echo "Unable to read output variable from step download-public"; exit 1
          fi

          if [ -n "$PRIVATE_TAG_NAME" ]; then
            echo "$PRIVATE_TAG_NAME"
          else echo "Unable to read output variable from step download-private"; exit 1
          fi

          echo "Downloaded files:"
          echo "$DOWNLOADED_FILES"
